function Word(arr) {
    this.arr = arr;
    this.stringRep = function() {
        letters.join;
    };
    this.guess = function() {
        letters.array.forEach(element => {
            guess(element);
        });
    };
};

module.exports = Word;